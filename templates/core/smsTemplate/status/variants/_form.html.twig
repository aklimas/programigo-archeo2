<div class="row">
    <div class="col-12 col-lg-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Status <span>| SmsTemplate</span></h5>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-sm btn-primary">{{ button_label|default('Zapisz') }}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

