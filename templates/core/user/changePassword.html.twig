{% extends 'panel.html.twig' %}
{% set title = 'Zmiana hasła' %}
{% set header_title = 'Zmiana hasła' %}
{% block body %} {{ parent() }}
    <div class="row justify-content-center">
        <div class="col-6">
            <div class="card shadow-sm ">
                <div class="card-header">
                    <h6 class="my-2">
                        <i class="fas fa-user-lock mr-1 text-muted"></i> Zmiana hasła
                    </h6>
                </div>
                <div class="card-body">
                    {{ form(formPassword) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
